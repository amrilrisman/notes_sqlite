import 'package:flutter/material.dart';

class ContentNoteDraw extends StatelessWidget {
  const ContentNoteDraw({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Column(
      children: [],
    ));
  }
}
